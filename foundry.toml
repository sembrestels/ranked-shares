[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.28"
optimizer = true
optimizer_runs = 200
# The Arkiv witness path takes Noir past EIP-170 with legacy code generation.
# Keep generated verifier assembly on its original compiler pipeline.
additional_compiler_profiles = [{ name = "noir-ir", via_ir = true, optimizer_runs = 1 }]
compilation_restrictions = [
    { paths = "src/noir/NoirRankedShares.sol", via_ir = true },
    { paths = "src/uniswap/LPCreRankedShares.sol", via_ir = true },
    { paths = "src/noir/lib/Poseidon2.sol", via_ir = false },
]
ffi = true
fs_permissions = [
    { access = "read", path = "./reference" },
    { access = "read", path = "./noir/proofs" },
    { access = "read", path = "./zisk/fixtures" },
    { access = "read-write", path = "./zisk/proofs" },
]
gas_reports = ["PBEAR", "RankedShares", "NoirRankedShares", "Poseidon2"]
gas_limit = "18446744073709551615"
remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/",
    "forge-std/=lib/forge-std/src/",
]

[fmt]
# The Honk verifiers are generated by noir/scripts/build.sh and the ZisK verifier is
# vendored from the PLONK key; formatting them would make every rebuild show up as a diff.
ignore = [
    "src/noir/verifiers/*.sol",
    "test/noir/verifiers/*VerifierTest.sol",
    "src/zisk/IZiskVerifier.sol",
    "src/zisk/PlonkVerifier.sol",
    "src/zisk/ZiskVerifier.sol",
]

[fuzz]
runs = 256
