# CRE project settings. One block per target; select with `cre … --target <name>`.
# RPC URLs may reference environment variables as ${VAR_NAME} (resolved from cre/.env or the shell).
staging-settings:
  rpcs:
    - chain-name: arc-testnet
      url: https://rpc.testnet.arc.network
