# CRE workflow settings for the sealed-ballot tallier. Paths are relative to this folder.
staging-settings:
  user-workflow:
    workflow-name: "ranked-shares-sealed-staging"
  workflow-artifacts:
    workflow-path: "../../src/main.ts"
    config-path: "./config.staging.json"
    secrets-path: "./secrets.yaml"
