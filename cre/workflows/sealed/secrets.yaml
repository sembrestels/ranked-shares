# Secret ids the workflow reads with runtime.getSecret({ id }), mapped to the environment
# variable that supplies the value (simulation: exported in the shell; deployment: cre secrets create).
secretsNames:
  RANKED_SHARES_MASTER:
    - CRE_RANKED_SHARES_MASTER
